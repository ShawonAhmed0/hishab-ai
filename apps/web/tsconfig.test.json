{
  "// note": "The test files the app's tsconfig deliberately excludes. Run by `npm run typecheck`, never by `next build`.",
  "extends": "./tsconfig.json",
  "include": ["next-env.d.ts", "vitest.setup.ts", "**/*.test.ts", "**/*.test.tsx"],
  "exclude": ["node_modules"]
}
